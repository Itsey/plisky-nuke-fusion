namespace Plisky.Nuke.Fusion;
public static partial class GenericSettingsExtensions {

    /*
    public static T SetProcess<T>(this T toolSettings, string processName, bool tryAutoResolve = true) where T : GenericSettings {
        toolSettings = toolSettings.NewInstance();
        toolSettings.TryAutoResolve = tryAutoResolve;
        toolSettings.GenericExeName = processName;
        return toolSettings;
    }


    public static T SetArgumentString<T>(this T toolSettings, string argumentList) where T : GenericSettings {
        toolSettings = toolSettings.NewInstance();

        toolSettings.DirectSetArguments = argumentList;
        return toolSettings;
    }

    public static T SetWorkingDirectory<T>(this T toolSettings, string workingDirectory) where T : GenericSettings {
        toolSettings = toolSettings.NewInstance();

        toolSettings.WorkingDirectory = workingDirectory;
        return toolSettings;
    }
    */
}
